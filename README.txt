Parallel program to simulate the Ising and Potts' models in various geometries

modules needed to compile: cports, gcc, intel and openmpi

how to compile: code should compile just by typing 'make' (on Chuck)

make clean: removes executables as well the .o files and the .txt files
